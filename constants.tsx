
import * as React from 'react';
import { EventCategory } from './types';

export const COLORS = {
  primary: '#1C1C1E',
  accent: '#D4AF37',
  background: '#E6DDD3',
  dark: '#121212',
};

export const DEFAULT_TRIP_CONFIG = {
  headerImagePosition: 50,
  budget: 50000,
};

export const APP_VERSION = "v0.1.0-sync-fix";
